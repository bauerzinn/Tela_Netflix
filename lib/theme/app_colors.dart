import 'package:flutter/material.dart';

/// Cores principais da interface (estilo streaming escuro).
abstract final class AppColors {
  static const bg = Color(0xFF1A0A0A);
  static const bottomBar = Color(0xFF0D0D0D);
  static const netflixRed = Color(0xFFE50914);
}
